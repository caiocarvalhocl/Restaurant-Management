# Copy to .env and fill in real values. .env is gitignored and never committed.

# JWT signing key. Must be set — the app refuses to start without it.
# Generate one with: openssl rand -base64 64
JWT_SECRET=

# Postgres credentials used by docker-compose (adjust if you change them there too)
POSTGRES_PASSWORD=restaurant123
